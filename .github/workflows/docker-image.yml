name: Build and publish

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: emmamau/cnamApp

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    concurrency:
      cancel-in-progress: true
      group: ${{ github.workflow }}-${{ github.ref }}

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - run: echo "Hello, world!"
